document.addEventListener('DOMContentLoaded', function () {
    // Manipulación del DOM dinámicamente
    document.getElementById('demo').innerHTML = 'Texto generado dinámicamente con JavaScript';
  
    // Utilización de funciones anónimas
    (function () {
      // Código de la función anónima
    })();
  });
  